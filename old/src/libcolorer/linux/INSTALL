
  Colorer-take5 library linux package.
  
  To install:
  
  1. Run configure:

     './configure' or
     './configure --prefix=/usr'
       The former shall use default install location
       (/usr/local/share, /usr/local/lib, /usr/local/share/doc)
       And the latter shall use a specified one.

  2. Run build:
     
     'make'
     
  3. Run install:
    
     'make install'
     